name: Build macOS App (Unsigned Final)

on:
  push:
    branches: ["main"]
  workflow_dispatch:

jobs:
  build-macos:
    runs-on: macos-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # --- Ensure binaries and configs are present ---
      - name: Verify bin and config folders
        run: |
          echo "🧩 Checking local binaries and configs..."
          ls -lh bin/ || (echo "❌ bin folder missing!" && exit 1)
          ls -lh config/ || (echo "❌ config folder missing!" && exit 1)

          # Make sure binaries are executable
          chmod +x bin/ffmpeg bin/ffprobe bin/yt-dlp
          echo "✅ All binaries marked executable."

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pyinstaller PySide6

      - name: Build macOS app
        run: |
          echo "🚀 Building unsigned macOS app..."
          pyinstaller build_macos.spec
          echo "✅ Build completed."

      # --- Verify that build outputs are valid ---
      - name: Verify artifact contents
        run: |
          echo "🔍 Verifying build contents..."
          echo ""
          echo "==> .app structure:"
          find dist/WebTvMux.app -maxdepth 4 -type f | sort
          echo ""
          echo "==> Embedded binaries:"
          find dist/WebTvMux.app/Contents/MacOS/bin -type f -exec ls -lh {} \; || echo "⚠️ bin folder missing!"
          echo ""
          echo "==> Config files:"
          find dist/WebTvMux.app/Contents/MacOS/config -type f -exec ls -lh {} \; || echo "⚠️ config folder missing!"
          echo ""
          echo "==> Bundle size:"
          du -sh dist/WebTvMux.app || true
          echo ""
          echo "==> DMG info:"
          ls -lh dist/WebTvMux.dmg || echo "⚠️ DMG not found!"
          echo ""
          echo "✅ Verification complete."

      # --- Optional sanity check: fail if .app is too small (<50 MB) ---
      - name: Validate app size
        run: |
          APP_SIZE=$(du -sm dist/WebTvMux.app | cut -f1)
          echo "App size: ${APP_SIZE} MB"
          if [ "$APP_SIZE" -lt 50 ]; then
            echo "❌ App size too small — likely missing ffmpeg/yt-dlp!"
            exit 1
          fi
          echo "✅ App size validation passed."

      # --- Upload artifacts for download ---
      - name: Upload macOS build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: WebTvMux-macos-build
          path: |
            dist/WebTvMux.app
            dist/WebTvMux.dmg
